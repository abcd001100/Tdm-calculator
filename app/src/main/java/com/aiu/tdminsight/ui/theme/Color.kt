package com.aiu.tdminsight.ui.theme

import androidx.compose.ui.graphics.Color

// A clinical teal accent, distinct from Material's default purple, with a
// muted slate neutral and an amber reserved for warnings/blocked states
// (validation errors, the calculation engine's blocked status, etc.) —
// kept separate from the primary accent rather than reusing it.
val TealPrimaryLight = Color(0xFF0E7C66)
val TealPrimaryDark = Color(0xFF43C0A0)
val SlateNeutral = Color(0xFF55635B)
val AmberWarning = Color(0xFFA6690A)
