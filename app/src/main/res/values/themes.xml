<resources>
    <!-- Base application theme. The app itself is Material 3 via Jetpack
         Compose (see ui/theme/Theme.kt); this system theme only covers
         the window background shown before Compose takes over. -->
    <style name="Theme.TdmInsight" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
